/**
 * @arch.id     addone.core
 * @arch.parent addone
 * @arch.role   system
 */
// src/: the TypeScript core, run directly on Node.
/** Skeleton marker. Every unimplemented body throws through here so a stub can never pass as done. */
export function todo(what: string): never {
  throw new Error(`TODO: ${what}`);
}
